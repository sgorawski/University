<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="{{ url_for("wallets.index") }}">
        Crypto Wallets Management
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <div class="navbar-nav mr-auto"></div>
        <div class="navbar-nav">
            {% if is_authenticated() %}
                <span class="nav-item navbar-brand">
                    {{ get_text() }}
                </span>
                <form method="post" action="{{ url_for('wallets.logout') }}">
                    <input class="btn btn-outline-secondary" type="submit" value="Exit" />
                </form>
            {% else %}
                <span class="nav-item navbar-brand">
                    No wallet selected
                </span>
                <a class="nav-item btn btn-outline-secondary" href="{{ url_for("wallets.login") }}">
                    View wallet
                </a>
            {% endif %}
        </div>
    </div>
</nav>
