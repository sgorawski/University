fun power ( x : int * int ) : int =
	let a = fst x in
	 let b = snd x in
		if b = 0 then 1
			else let c = power ( a, b div 2 ) in
				if b mod 2 = 1 then c * c * a else c * c
			  
input a b in power (a,b)
